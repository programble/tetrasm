%include "video.mac"

%define well.INSIDE.WIDTH 20
%define well.INSIDE.HEIGHT 20

%define well.WIDTH (well.INSIDE.WIDTH + 8)
%define well.HEIGHT (well.INSIDE.HEIGHT + 3)

%define well.X (COLS / 2 - well.WIDTH / 2)
%define well.Y 0

%define well.INSIDE.X (well.X + 4)
%define well.INSIDE.Y (well.Y + 2)
